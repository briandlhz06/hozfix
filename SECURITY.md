# Seguridad

Hozfix imprime comandos que después corrés vos, casi siempre con sudo. Leé el plan. Si toca SSH, dejá otra sesión abierta.

Si el JSON de entrada puede inyectar un comando, o un fix te deja afuera del server sin decirlo, mail a briandlhz06@proton.me con "hozfix" en el asunto. Mandá el JSON (sin secretos) y qué salió.

Que falte una receta es un issue.

Arreglos en `main`.
